const express = require("express"); const router = express.Router(); const { pool } = require("../db"); router.get("/setup-db", async (req, res) => { try { await pool.query("CREATE TABLE IF NOT EXISTS users (id SERIAL PRIMARY KEY, full_name VARCHAR(100), email VARCHAR(100) UNIQUE NOT NULL, password_hash VARCHAR(255) NOT NULL, phone VARCHAR(30), country VARCHAR(60), balance DECIMAL(15,2) DEFAULT 0, total_profit DECIMAL(15,2) DEFAULT 0, referral_earnings DECIMAL(15,2) DEFAULT 0, referral_code VARCHAR(10) UNIQUE, referred_by INT, current_plan VARCHAR(20), kyc_status VARCHAR(20) DEFAULT 'unverified', is_active BOOLEAN DEFAULT true, role VARCHAR(10) DEFAULT 'user', created_at TIMESTAMP DEFAULT NOW())"); await pool.query("UPDATE users SET role='admin' WHERE email='admin@nexvault.io'"); const r = await pool.query("SELECT id,email,role FROM users WHERE email='admin@nexvault.io'"); res.json({ message: "DONE!", admin: r.rows[0] }); } catch(e) { res.status(500).json({ error: e.message }); } }); module.exports = router;
